# LATTICE code budget — enforced by scripts/check_code_budget.sh
#
# Signals we enforce (user- and contributor-facing):
#   - Per-directory caps (local inflation)
#   - No file >800 LoC in src/lattice/
#   - enforce_total_v2 aspirational cap until Phase 31 (Rust core migration)
#
# We do NOT enforce per-phase net LoC deltas (removed — measured accounting, not health).
# Shrink comes from transport consolidation (Phase 14), dedup gates, and Rust core (Phase 31).

total_v2=35000
enforce_total_v2=0
enforce_dir_caps=1

# Per-directory caps (baseline +5% at Phase 13; ratchet down in owning phase PRs)
core=1676
ir=4217
transforms=8343
pipeline=3358
planner=2496
cache=1139
safety=501
agent=2100
transport=1883
providers_adapters=3024
proxy=1718
# gateway/compat split: ~4571 actual; shrink target Phase 14 + Phase 21 — bump only with justification
gateway=4825
integrations=5225
telemetry=1862
state=1127
audit=840
auth_keys_quotas_tenants=2940
mcp=1575

# Non-Python (informational until Phase 31)
crates_lattice_core=6000
bindings_python=1000
bindings_wasm=500
typescript_sdk_src=2500
cursor_extension_src=800
